server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # React Router: send unknown paths back to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
